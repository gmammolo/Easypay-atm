import { FilterMovementsPipe } from './filter-movements.pipe';

describe('FilterMovementsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMovementsPipe();
    expect(pipe).toBeTruthy();
  });
});
